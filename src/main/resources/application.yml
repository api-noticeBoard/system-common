# ===================================================================
# system-common ????? ?? ?? (Default Configuration)
# ? ?????? ????? ???? ??????(?: board-api)?
# ??? application.yml ???? ? ???? ?????(override)??? ??? ? ????.
# ===================================================================

# --- Spring ?? ?? ---
spring:
  # ??????? ??? ?????.
  # Eureka, Spring Cloud Config ?? ??? ?????? ?? ?? ?? ???? ??????? ???? ID? ?????.
  # ?? ???? ? ??? ???? ?? ??????? ???? ?? ??? ? ????.
  # **??**: ? ?????? ???? ?? ???????? ??? ??? ???? ???? ???? ???.
  application:
    name: ${app.name:system-common}

  # --- Redis ?? ?? ---
  # ? ?????? CacheConfig(@EnableCaching)? ???? ?? ??? Redis ?? ?? ?????.
  # ?? ??????? ??(??, ??, ??)? ?? ? ?? ???? ???.
#  data:
  redis:
    host: localhost # Redis ??? ??? ??
    port: 6379      # Redis ??? ?? ??
    database: 1  # ??? ?? DB ???
      # password: your-redis-password # Redis? ????? ??? ?? ??? ???? ?????.
#    cache:
#      type: redis

# --- ??(Logging) ?? ---
logging:
  # ????? ?? ??? ?????.
  # ?? ?? ??: TRACE > DEBUG > INFO > WARN > ERROR
  # ?? ??? ????, ? ?? ??? ??? ?????. (?: INFO? ?? ? INFO, WARN, ERROR ??? ??)
  level:
    # ??? ?? ?? ?????(system-common)? ??? DEBUG ??? ????,
    # ?? ?? AOP ?? ? ??? ?? ??? ??? ? ??? ???.
    com.portfolio.common: debug
    # Spring ????? ??? ??? INFO ??? ????, ?? ??? ??? ???? ?? ?????.
    org.springframework: info
    org.springframework.cache: debug
    # (?? ? ??? ?? ??)
    # org.hibernate.SQL: debug  # JPA(Hibernate)? ???? SQL ??? ??? ?? ?? ? ?????.
    # org.hibernate.type.descriptor.sql: trace # SQL? '?'? ????? ???? ??? ???? ?? ?? ? ?????.

  # ??? ??? ??? ?????.
  pattern:
    # ??(Console)? ??? ??? ?????.
    # %d{yyyy-MM-dd HH:mm:ss.SSS} : ?? ? ??
    # [${spring.application.name:}] : ?????? ?? (???? ?? ?? ????)
    # [%X{traceId:-}] : MDC? ??? traceId? ?????. ??? '-'? ?????. (TraceIdFilter? ??)
    # [%t] : ?? ???? ??
    # %-5level : ?? ?? (INFO, DEBUG ?). -5? 5?? ??? ???? ?? ?????.
    # %logger{36} : ??(???)? ??. {36}? ?? ??? 36?? ?????.
    # - %msg%n : ?? ?? ???? ???
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [${spring.application.name:}] [%X{traceId:-}] [%t] %-5level %logger{36} - %msg%n"

# --- Spring Boot Actuator ?? (????) ---
# Actuator? ??????? ??? ?????? ??? ? ?? HTTP ?????? ?????.
# (?: /actuator/health ? ???? ??????? ?? ????? ??)
management:
  endpoints:
    web:
      exposure:
        # ??? ?? ?????? health? info ?????? ??? ?????.
        # Prometheus ?? ???? ??? ???? ?? metrics, prometheus ?????? ??? ?????.
        include: "health,info,metrics,prometheus"
  endpoint:
    health:
      # /actuator/health ?? ?, DB ?? ?? ? ?? ??? ???? ?????.
      # when_authorized: ??? ?????? ?? ?? ??
      # always: ?? ?? ?? ?? (??? ??)
      # never: ?? ?? ?? ?? ? ?
      show-details: when_authorized

# --- Resilience4j ?? (?? API ?? ???) ---
# HttpClient?? ??? @CircuitBreaker(name="external"), @Retry(name="external")? ??
# ????? ?? ?????. ??? ??? ?? ResilienceConfig? ??? ?? ?? ??? ?????(override).
resilience4j:
  circuitbreaker:
    instances:
      # "external"??? ??? ?? ???? ????? ?? ??
      external:
        register-health-indicator: true # /actuator/health ?????? ? ?? ????? ??(UP, DOWN ?)? ??????.
        # (????? ????? ??)
        # ResilienceConfig? ?? ??? ???? ?? ? ??? ?????.
        # failure-rate-threshold: 60 # external API? ???? ??? 60%? ???? ?? ?? ?
  retry:
    instances:
      # "external"??? ??? ??? ????? ?? ??
      external:
      # (????? ????? ??)
#       max-attempts: 5 # external API? ???? ?? 5? ??? ?? ?? ?